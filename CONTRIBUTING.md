# Contributing to OpenRating
- Use Conventional Commits (`feat:`, `fix:`, etc.).
- Run `npm run build` and `npm run lint` in `service/ts` before PRs.
- Keep PRs focused; include tests where relevant.
