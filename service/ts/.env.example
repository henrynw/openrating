PORT=8080
DB_HOST=localhost
DB_NAME=ratings
DB_USER=ratings_admin
DB_PASS=changeme
AWS_REGION=eu-west-1
LEDGER_BUCKET=local-ledger-bucket
INSIGHTS_WORKER_POLL_MS=1000
INSIGHTS_WORKER_BATCH_SIZE=25
INSIGHTS_WORKER_LOOP_COOLDOWN_MS=25
INSIGHTS_WORKER_LOG_EVERY=250

# Authentication
# Set AUTH_PROVIDER=DEV to enable the shared-secret dev signer.
#AUTH_PROVIDER=DEV
#AUTH_DISABLE=0
#AUTH_DEV_SHARED_SECRET=change-me
#AUTH_DEV_AUDIENCE=http://localhost:8080
#AUTH_DEV_ISSUER=openrating-dev

# Set AUTH_PROVIDER=COGNITO to verify AWS Cognito access tokens.
#COGNITO_REGION=us-east-1
#COGNITO_USER_POOL_ID=us-east-1_example
#COGNITO_AUDIENCE=your-app-client-id
